npm-debug.log
node_modules
doc
.DS_Store
.idea
*.iml
*.ipr
*.iws
nbproject
package-lock.json
yarn.lock
# NOTE: /build and /js are intentionally committed so this fork installs as a
# prebuilt git dependency (no `prepare` build in consumers). Rebuild with
# `npm run build` and commit the output whenever src/ changes.
